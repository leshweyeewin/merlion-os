# 📊 Live Data Dashboard & Exact Data Sources

All data panels in the **SG Hub Dashboard** load on-demand when clicked and show a **"Last synced"** SGT timestamp. Below are the exact sources and APIs feeding the UI:

| UI Sub-Panel | Data Source / API Endpoint | Display Details |
|---|---|---|
| 🌤️ **Weather & Air Quality** | **NEA API** (`https://api-open.data.gov.sg/v2/real-time/api/`: `psi`, `pm25`, `two-hr-forecast`, `twenty-four-hr-forecast`, `air-temperature`, `relative-humidity`, `wind-speed`, `wind-direction`, `rainfall`, `uv-index`) | Visual PSI gauge + 6-region 2-hour forecast cards, PM2.5, live "Current Conditions" tiles (temperature, humidity, wind speed/direction via circular mean, rainfall, UV Index with NEA 5-tier scale), and a 24-hour general outlook. |
| 🚇 **Transit & Rail Alerts** | **LTA DataMall API** (`TrainServiceAlerts`) + **ICA Newsroom Dynamic Endpoint** (`https://www.ica.gov.sg/ICAContentInterface/MediaReleasesList/FindMediaReleases`) | Live line-by-line status grid (EWL, NSL, NEL, CCL, DTL, TEL, LRTs) with disruption logs, free public bus boarding notices, and free MRT shuttle routes. Also includes a dedicated **ICA Checkpoint & News Updates** panel listing real-time border checkpoint advisories (e.g. Woodlands/Tuas delays) and media releases. |
| 🚕 **Transport & Vehicle Costs** | **LTA DataMall API** (`Taxi-Availability`) + **data.gov.sg Dataset API** (COE Bidding Results / Prices, `d_69b3380ad7e51aff3a7dcc84eba52b8a`) | Live islandwide taxi count and the latest COE bidding premiums for all 5 vehicle categories (A–E), each with a **demand-momentum read** — round-over-round premium change plus a bids-to-quota oversubscription ratio (e.g. "1.51x bids/quota — fierce bidding"), so a category can't look flat purely off one round's premium while bidding pressure is quietly building underneath. A rule-based **"why" explanation** compares quota and bid-volume changes round-over-round to say whether a premium move was a supply story, a demand story, or both (shown only when either moved ≥5%). An "Around You (2km)" button (browser geolocation, requested only on click) shows a live nearby count within 2km plus the nearest planning area name, reverse-geocoded against a built-in list of Singapore's major towns. |
| 📢 **Gov Updates** | **Telegram Scraper** (22 Channels: `@govsg`, `@HealthHubSG`, `@scamshieldalert`, `@LTAsg`, `@NEAsg`, `@MOEsg`, `@GovTechSG`, `@MOHSingapore`, `@SPFsg`, `@SCDFsg`, `@momsg`, `@ReachSingapore`, `@ElectionsDepartmentSingapore`, `@neasingapore`, `@skillsfuturesg`, `@Skills_Workforce_Development`, `@CPFBoard`, `@LTASingapore`, `@govtechbytes`, `@NLBsg`, `@urasingapore`, `@irassg`) + a dedicated **CDC / RedeemSG Media Releases** card (BeautifulSoup scrape of the `cdc.gov.sg` newsroom — CDC has no Telegram channel) + **PUB Flood Alerts API** (`https://api-open.data.gov.sg/v2/real-time/api/weather/flood-alerts`) | All broadcasts posted strictly within the **last 3 days (72 hours)** streamed per channel and sorted chronologically descending by SGT post date. Quiet channels inside that window return 0 posts so historical posts do not contaminate the 72-hour feed; if zero broadcasts are returned across all channels in 3 days, an automatic single-latest-post fallback triggers with a "Showing last known data" status pill. A **window hint chip** (`🕐 Official broadcasts from the last 3 days (72 hours) · N posts`) surfaces the active count. |
| 🏢 **HDB BTO Tracker** | **HDB Pulse & Newsroom Scraper** (`https://www.hdb.gov.sg/hdb-pulse/news`) + **Enhanced CPF Housing Grant (EHG) Estimator** | Live BTO launch tables + BeautifulSoup HDB newsroom Next.js `__NEXT_DATA__` JSON extraction (resolving real CMS URLs dynamically), combined with an instant grant estimator based on household income. |
| 🏷️ **HDB Resale Flat Prices** | **Google BigQuery** (`sg_housing.hdb_resale_prices`, loaded via `scripts/load_hdb_resale_to_bigquery.py`) with automatic fallback to the **data.gov.sg Dataset API** (Resale Flat Prices, `d_8b84c4ee58e3cfc0ece0d773c8ca6abc`) → disk snapshot; + **CPF HDB Accrued Interest Calculator** | Real islandwide median resale price, YoY trend, ranked town breakdown for the latest complete month, and an interactive calculator to estimate compounding accrued interest (2.5% annually) to be returned to CPF OA upon property sale. The 236k-row dataset is answered by two small BigQuery queries (latest two months for an exact median; a `GROUP BY month` `APPROX_QUANTILES` roll-up for the trend chart) instead of a ~20MB CSV download, memoised for hours. A rule-based **"why" explanation** compares each flat type's own YoY median change against the headline islandwide figure to flag when the reported change is broad-based across flat types versus largely a mix-shift (e.g. more executive flats transacting, not genuine price movement). |
| 📊 **Job Market Analysis** | **Google BigQuery** (real MOM Job Vacancy by Industry & Occupation data, loaded via `scripts/load_job_vacancy_to_bigquery.py`), with automatic fallback to a direct **data.gov.sg Dataset API** call if BigQuery isn't configured; cross-referenced against the **MOM Retrenched Employees by Industry** dataset (`d_61d92d31ca400be135190614277da825`) for the Hiring Pressure Index below | Real vacancy counts, YoY trend, and a next-year forecast per sector (Tech, Finance, Healthcare, General), plus an interactive **multi-year vacancy trend line chart** (2014→latest, hover crosshair with exact counts per sector). Also computes a **Hiring Pressure Index** — same-year vacancies ÷ retrenchments in the same industries — so a sector can't look healthy purely off a positive YoY delta while it's shedding workers just as fast; classified strong/moderate/balanced/weak, or flagged as pure hiring growth when zero retrenchments are recorded. And a **multi-year CAGR trend-break check** — compares this year's YoY change against the sector's own CAGR over the fetched window (up to 6 years) and flags when the two diverge by ≥3pp, so a positive YoY figure sitting on top of a much steeper (or shallower) multi-year trend reads as deceleration/acceleration rather than a flat percentage. A rule-based **"why" explanation** cross-references the trend-break verdict against the Hiring Pressure Index — e.g. distinguishing "accelerating because of genuine net hiring demand" from "accelerating but mostly churn, since retrenchments are rising just as fast" — shown only when both readings are available and the divergence is meaningful. All displayed figures are real; the former illustrative median-salary/skills/risk fields were removed. |
| ⚠️ **MOM Retrenchment** | **data.gov.sg Dataset API** (MOM Retrenched Employees by Industry, Quarterly, `d_61d92d31ca400be135190614277da825`) | Real latest-quarter retrenchment headcount and top affected industries, plus a **24-quarter retrenchment trend chart** with an auto-computed "vs recent average" takeaway. (The former illustrative six-month re-employment rate was removed.) |
| 💼 **Occupational Wage Explorer** | **Google BigQuery** (`sg_employment.occupational_wages`, loaded via `scripts/load_ows_to_bigquery.py`) with automatic fallback to the live **MOM Occupational Wage Survey Excel tables** (stats.mom.gov.sg "Occupational Wages Tables" page, table1 T1 sheet, two most recent June editions) → committed seed. BigQuery is the primary tier here specifically because stats.mom.gov.sg **WAF-403s datacenter IPs** — loading the parsed rows into BigQuery from an allowed IP lets the deployed app query them without hitting that block | Insights-first breakdown of 500+ detailed job titles: key takeaways summary, a **wage-distribution histogram** (how many titles per salary band, with the cross-title median), a **pay-vs-raise scatter plot** (every occupation plotted by wage level × YoY change, tech/digital roles highlighted, per-dot hover tooltips), top 5 wage risers/decliners, top 5 highest-paying tech & digital roles, notable genuinely new (SSOC 2024 / AI-era) job titles (renamed titles fuzzy-matched to their old rows), and a search-driven wage lookup that defaults to a top-10 leaderboard. A rule-based **"why" explanation** compares tech/AI roles' average YoY wage change against the rest of the workforce to flag whether the AI-era wage story is real pay growth or mostly just new job creation (shown only when the gap is ≥2pp with sufficient title coverage on both sides). (The former 25th/75th percentile enrichment from the one-off June 2024 data.gov.sg edition was removed under the same freshness policy.) |
| 🎟️ **Kiasu SG Deals** | **Telegram Scraper** (18 Channels: `@confirmgood`, `@goodlobang`, `@kiasufoodies`, `@sgweekend`, `@moneydigest`, `@goodyfeedsg`, `@TSLMedia`, `@todayonlinesg`, `@dailyvanity`, `@triptalksSG`, `@dateideas`, `@klooktravelsg`, `@youtripsg`, `@sgnewmovies`, `@greatdealssg`, `@danielfooddiary`, `@allsgpromo`, `@sgmrt`) | Community deals and lifestyle news posted strictly within the **last 24 hours**, sorted newest-first. |
| ⚖️ **IRAS Tax & Wealth** | **IRAS Due Dates Scraping Endpoint** (`https://www.iras.gov.sg/due-dates`) + **IRAS Latest Updates Web Scraper** (`https://www.iras.gov.sg/latest-updates`) + **Personal Tax Relief Optimizer** (progressive resident brackets YA 2024–2026) | Official chronological filing calendar deadlines (Individual, Corporate, GST) scraped live from IRAS, a live **IRAS Latest Updates** card pulling official tax announcements directly from `https://www.iras.gov.sg/latest-updates`, plus an interactive CPF Special Account (RSTU) vs. SRS Top-up Optimizer with dollar-saved calculator and statutory $80,000 relief cap advisories. See [iras_optimizer.md](iras_optimizer.md). |

## 🩺 Automated data-source monitoring

The scrapers above all **degrade silently** — if a source changes its HTML/JSON, the parse comes back empty and the pane quietly serves committed sample data (flipping an internal `is_live` flag) rather than crashing. To catch that, [`scripts/healthcheck.py`](../scripts/healthcheck.py) runs on a daily schedule via the [`Data source monitor`](../.github/workflows/monitor.yml) GitHub Action:

- **Scraper canaries** — re-runs each live scraper (HDB Newsroom, HDB BTO launch tables, ICA Newsroom, IRAS due dates, IRAS latest updates) from a GitHub-runner IP (which the `.gov.sg` WAFs allow) and **structurally validates** the result: right shape, non-empty, expected fields present. A renamed key or changed CSS class → empty parse → caught (something a plain HTTP-200 check would miss).
- **BigQuery health + staleness** — for each of the 3 loaded tables (`sg_housing.hdb_resale_prices`, `sg_employment.job_vacancy_by_industry`, `sg_employment.occupational_wages`) it verifies the table exists, has a sane row count, and — reading `MAX(period)` straight from the table — how many months/years behind "today" the freshest loaded period is. Past each dataset's lag tolerance it flags **"a newer edition is likely out — re-run the loader"**, the actionable "new data available" signal.

On any failure or staleness warning the workflow opens (or refreshes) a single labelled `data-monitor` GitHub issue with the full report, and auto-closes it once a later run is all-green. No external service and no new secrets — it reuses the `GCP_SA_KEY` the deploy already uses and the built-in `GITHUB_TOKEN`.

## 🔔 Watchlists & alerts

The **My Alerts** tab (in the SG Hub) lets a visitor subscribe to a threshold on any signal the dashboard already tracks and be notified only when it crosses — turning the dashboard from something you check into something that reaches out. Backed by [`tools/alerts.py`](../tools/alerts.py) (SQLite store + evaluator) and [`tools/alert_delivery.py`](../tools/alert_delivery.py) (delivery adapters).

| Watch type | Fires when | Source (reused, cached) |
|---|---|---|
| **COE premium** | a category's premium goes below/above your threshold | `compute_coe_bidding_stats()` |
| **PSI / air quality** | national PSI crosses your threshold | `fetch_weather_data()` |
| **MRT line** | LTA reports a disruption on your line | `fetch_lta_train_alerts()` |
| **HDB resale** | your town's median moves ±X% from when you subscribed | `compute_hdb_resale_stats()` |
| **BTO launch** | a launch appears in your town | HDB newsroom scrape |
| **IRAS deadline** | a filing deadline falls within N days | `fetch_iras_due_dates()` |

- **One evaluator, three channels.** A daemon thread sweeps every ~5 min (`ALERTS_INTERVAL_SECONDS`), reusing the same memoised payloads the panels serve (so it adds no upstream load), records an in-app notification, and fans out to the user's linked **Web Push** and **Telegram** channels. **State-based dedupe** means a standing condition (PSI stuck above 100) fires once, not every sweep.
- **No accounts.** Identity is a browser-generated `client_id` in localStorage; every request is scoped to it. Telegram binds to the same id via a 6-digit **pairing code**; Web Push binds a `PushManager` subscription.
- **Telegram bot (two modes, one handler).** The bot (`tools/telegram_bot.py`) links a chat when the user sends the pairing code (or taps the `t.me/<bot>?start=<code>` deep link). It runs either by **long-polling** (`TELEGRAM_MODE=polling`, the default — a daemon thread, no public URL, ideal for local dev) or by **webhook** (`TELEGRAM_MODE=webhook` + `POST /api/alerts/telegram/webhook`, for a public HTTPS host like Render; register it with `py -3 scripts/telegram_setup.py set <url>` and authenticate it with `TELEGRAM_WEBHOOK_SECRET`). Commands: `/start [code]`, a bare 6-digit code, `/stop` (unlink), `/help`.
- **Config (all optional, graceful when absent):** `VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY`/`VAPID_SUBJECT` for Web Push (generate with `py -3 scripts/gen_vapid_keys.py`), `TELEGRAM_BOT_TOKEN`/`TELEGRAM_BOT_USERNAME` (+ `TELEGRAM_MODE`, `TELEGRAM_WEBHOOK_SECRET`) for Telegram. Without them the in-app feed still works; the extra channels simply stay hidden/disabled. Storage path is `$ALERTS_DB_PATH` (default `data/alerts.db`; attach a Render persistent disk to survive redeploys).

## 🛡️ Scam checker

Paste a suspicious SMS / message / URL → a heuristic risk verdict (`tools/scam_checker.py`, endpoint `POST /api/scam/check`, UI in the **Scam Checker** SG Hub tab, plus the Telegram bot).

- **Signals.** Brand **impersonation** is the strongest — a message name-dropping Singpass / a bank / IRAS / SingPost but linking to a domain that isn't that org's real one (the brand token baked into the host, or the brand named while the link is independently suspicious). Plus URL shorteners, lookalike/**punycode** domains, raw-IP hosts, throwaway TLDs (`.xyz`, `.top`, …), `http`, and text signals (urgency/threats, OTP/credential asks, prize/parcel/refund lures, off-platform contact). Scores sum into `high`/`medium`/`low`/`none` tiers; impersonation or a campaign match short-circuits to `high`.
- **Trusts real `*.gov.sg`.** Nobody but a SG agency can register one, so a `gov.sg` link is never flagged as impersonation — a genuine IRAS message mentioning Singpass doesn't false-positive.
- **Campaign cross-reference.** Best-effort, cached (1 h) match against recent `@scamshieldalert` posts (`recent_scam_advisories()`), so a currently-circulating scam is flagged. Degrades to pure heuristics on any fetch failure.
- **Deterministic + offline engine.** `check(text)` uses no network/LLM, so it's testable and works even when rate-limited. Output is always **guidance, not certainty** — every verdict carries the same safe-action advice and a ScamShield (1799) report link, and a "looks ok" result never reads as "this is safe". Nothing pasted is stored.
